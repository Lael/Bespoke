import {EuclideanPolygon} from "../../../math/geometry/euclidean-polygon";
import {Vector2} from "three";

/**
 *
 * @param q a polygon
 * @param p a polygon
 * @return a sequence of points in phase space
 */
export function search2D(q: EuclideanPolygon, p: EuclideanPolygon): Vector2[] {
  // First, check for orbits of length 2 using faces and vertices

  // Then, check for orbits of length 3 using only faces
  
  return [];
}