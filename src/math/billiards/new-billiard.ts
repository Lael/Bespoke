export enum Duality {
    INNER = "Inner",
    OUTER = "Outer",
}

export enum Generator {
    LENGTH = "Length",
    AREA = "Area",
}

export enum Geometry {
    EUCLIDEAN = "Euclidean",
    HYPERBOLIC = "Hyperbolic",
    SPHERICAL = "Spherical",
}