import {Vector2} from "three";
import {Straight} from "./affine-billiard-table";

export function preimages(f: (p: Vector2) => Vector2, slicingRays: Straight[], iterations: number, frontier: Vector2[][]): Vector2[][] {
  return [];
}